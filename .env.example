# Copy to .env and fill in. .env is gitignored — keep it that way.
# Both keys come from Settings -> n8n API -> Create an API key on each instance.
# URLs live in n8n.config.json; set these only to override.
N8N_SELFHOSTED_API_KEY=
N8N_CLOUD_API_KEY=
